"use strict";
var date = new Date

export class Song extends Parse.Object {
  // title: string = ""
  // star: boolean = false
  // year: number = date.getFullYear()
  // month: number = date.getMonth() + 1
}
